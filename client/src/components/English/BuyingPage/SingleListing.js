import React from 'react';
import { Link } from 'react-router-dom';

import {
    Card,
    CardBody,
    CardFooter,
    CardTitle,
} from "reactstrap";

const SingleListing = (props) => {

    const oneListing = props.oneListing

    return (
        <Card className="card-plain">
            my card
        </Card>
    )
}

export default SingleListing